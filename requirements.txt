# Grounded — CPU-only RAG verification stack
sentence-transformers>=3.0      # bge embeddings + transformers/torch (verifier)
chromadb>=0.5                   # vector store
rank-bm25>=0.2.2                # lexical retrieval
requests>=2.31                 # Ollama client
fastapi>=0.115                 # API
uvicorn>=0.30                  # ASGI server (serves the SPA + API)
datasets>=2.0                  # Wikipedia corpus ingestion (GROUNDED_CORPUS=wikipedia)
